// Project control panel wiring.
//
// Rename is handled in index.js via rend.selectProject(), so there is no
// duplicate listener here. This stub is kept as the home for future
// control-panel behaviour (e.g. delete project).
export function controlPanel() {}
